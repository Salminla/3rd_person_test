using UnityEngine;
using Firebase.Auth;

public class FirebaseAuth : MonoBehaviour
{
    private FirebaseAuth auth;
    // Start is called before the first frame update
    void Start()
    {
        //auth = 
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    async private void HandleCreateNewAccountButton()
    {
        string userName;
        string password;
        
        //Task createNewUserTask = 
    }
}
